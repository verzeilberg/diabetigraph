<nav class="navbar navbar-expand-md navbar-light" id="menuHeader">
    <div class="navbar-collapse collapse w-100 order-1 order-md-0 dual-collapse2">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a href="{{ path('app_index') }}">
                    <img src="/logo.png" class="" alt="Diabetigraph" width="100vh" height="100vh"/>
                </a>
            </li>
        </ul>
    </div>
    <div class="navbar-collapse collapse w-100 order-3 dual-collapse2">
        <ul class="nav navbar-nav ml-auto">
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                       data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fas fa-user"></i>
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ path('app_userprofile') }}">
                            <i class="far fa-id-badge"></i>
                            {% trans %}Profile{% endtrans %}
                        </a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{ path('app_logout') }}">
                            <i class="fas fa-power-off"></i>
                            {% trans %}Log out{% endtrans %}
                        </a>
                    </div>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="btn btn-outline-dark" href="{{ path('app_login') }}">Login</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
